{
  "name": "antidote_ts_client_test",
  "version": "0.0.1",
  "description": "An experimental Antidote client for nodejs, written in typescript",
  "main": "./dist/main.js",
  "typings": "./dist/index",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "protobufjs": "5.0.1"
  },
  "devDependencies": {
    "proto2ts": "2.0.0",
    "rimraf": "^2.5.4",
    "typescript": "^2.0.3"
  },
  "scripts": {
    "compile": "tsc",
    "prebuild": "npm run compile",
    "postinstall": "typings install",
    "postcompile": "cp src/antidote_proto.d.ts lib/ && ts-npm-lint --fix-typings",
    "pretest": "npm compile",
    "test": "node dist/main.js",
    "clean": "rimraf lib/*",
    "watch": "tsc --watch",
    "lint": "tslint --project tsconfig.json"
  }
}
